# LRS Overlay — интерактивный оверлей для игр LRS

**LRS Overlay** — это расширение для Twitch, позволяющее зрителям трансляций быстро просматривать карточки персонажей и терминов из словаря прямо во время игры. Проект состоит из публичной страницы для зрителей и админ‑панели для редактирования данных без знания кода.

## Структура проекта

* **docs/** — основная папка с публичными файлами и админкой.
  * **index.html** — главный интерфейс для зрителей. Здесь герои распределены по колонкам «Живы», «Мёртвы» и «Неизвестно», а при клике открывается подробная карточка.
  * **viewer.js** — логика показа героев и словаря, сортировка по алфавиту и переключение темы.
  * **styles.css** — стили публичной страницы, вдохновлённые ChatGPT. Поддерживает светлую/тёмную темы и адаптивность.
  * **editor.html** — административная панель. Позволяет редактировать героев и словарь через формы, добавлять новых персонажей и скачивать готовые JSON‑файлы.
  * **editor.js** — логика админки: загрузка текущих данных, изменение статуса персонажей (жив, мёртв, неизвестно), управление темой и скачивание файлов.
  * **admin.css** — стили админки.
  * **data/** — исходные данные проекта:
    * **heroes.json** — карточки персонажей. В каждой записи есть поля `name`, `race`, `class`, `portrait`, `stats` и `status` (значения `alive`, `dead` или `unknown`), которые определяют в какую колонку отобразится герой.
    * **glossary.json** — глоссарий терминов. Термины сгруппированы по первой букве.
    * **settings.json** — дополнительные настройки внешнего вида (например, цвета) для публичной страницы.
  * **assets/** — изображения (портреты персонажей, фон). Добавляя сюда новые файлы, можно расширять набор доступных портретов.

## Как обновить данные

Большая часть данных хранится в файлах `heroes.json` и `glossary.json` в папке `docs/data`. Обновить их можно двумя способами: напрямую через GitHub или с помощью админки.

### Редактирование напрямую через GitHub

1. Откройте файл [`heroes.json`](https://github.com/kao820/twitch-overlay/blob/main/docs/data/heroes.json) или [`glossary.json`](https://github.com/kao820/twitch-overlay/blob/main/docs/data/glossary.json) в репозитории.
2. Нажмите на иконку ✏️ («Edit this file»).
3. Внесите изменения в нужные поля. Для героев убедитесь, что поле `status` принимает одно из значений: `"alive"`, `"dead"` или `"unknown"`.
4. Сохраните изменения через «Commit changes».

### Обновление через админ‑панель

Если вам удобнее работать в визуальном интерфейсе, используйте админку:

1. Перейдите по ссылке [админка](https://kao820.github.io/twitch-overlay/editor.html).
2. На вкладке **«Герои»** вы увидите карточки всех существующих персонажей. Здесь можно:
   * Изменять имя, расу, класс, портрет (URL) и характеристики.
   * Выставлять **статус** (Жив, Мёртв, Неизвестно) с помощью трёх кнопок под заголовком «Статус». От статуса зависит, в какую колонку герой попадёт на публичной странице.
   * Добавлять новые карточки кнопкой «Добавить». Появится пустая форма для заполнения.
   * Скачивать обновлённый файл **heroes.json** кнопкой «Скачать heroes.json». После скачивания откройте файл `heroes.json` на GitHub, удалите его содержимое и вставьте скачанный текст.
3. На вкладке **«Словарь»** можно:
   * Выбирать букву алфавита и видеть термины, сгруппированные по первой букве.
   * Редактировать или удалять термин (иконки ✎ и 🗑).
   * Добавлять новый термин, заполняя поля «Термин» и «Описание», затем нажимая «Добавить».
   * Скачивать файл **glossary.json** аналогичным образом.
4. В верхней части админки есть переключатель темы (☀️/🌙), который запомнит ваш выбор.

### Добавление портретов

Чтобы добавить изображения персонажей:

1. Откройте папку [`assets`](https://github.com/kao820/twitch-overlay/tree/main/docs/assets) в репозитории.
2. Нажмите **Add file → Upload files**.
3. Выберите изображения (рекомендуемый размер портрета — примерно 180 × 270 px, как в примерах). После загрузки используйте имя файла в поле «Портрет (URL)» в админке или в JSON.
4. Закоммитьте изменения.

## Публичный предпросмотр

Чтобы увидеть, как данные выглядят для зрителей, откройте [предпросмотр оверлея](https://kao820.github.io/twitch-overlay/). Здесь:

* Герои делятся на три колонки по статусу (Живы, Мёртвы, Неизвестно).
* Список героев сортируется по алфавиту.
* При нажатии на имя открывается карточка героя с портретом фиксированной ширины слева и подробной информацией справа.
* На вкладке «Словарь» есть поиск и алфавитный перечень терминов; по клику открывается описание термина.
* Переключатель ☀️/🌙 позволяет менять тему.

## Стиль

Для тёмной темы используются глубокие серые оттенки и зелёный акцент, для светлой — мягкие светлые тона. Вы можете изменить цвета через файл `settings.json` (текстовый цвет, цвет кнопок и панелей) и выбрать тему на лету через соответствующие переключатели.

---

P.S. Этот README отражает актуальное состояние проекта после внесения всех последних правок. Если вы вносите изменения в код или стиль, пожалуйста, обновляйте и этот файл, чтобы новые участники и пользователи понимали, как устроен оверлей и как с ним работать.

P. S. S. Да, он работает с Twitch. Обновить надо с тех времён приложение там, но [вот запись](https://disk.yandex.ru/i/t6jgFWwR8iSSeg) (в проекте по адресу [Оверлей.mp4](https://github.com/kao820/twitch-overlay/blob/main/%D0%9E%D0%B2%D0%B5%D1%80%D0%BB%D0%B5%D0%B9.mp4) тоже самое)
